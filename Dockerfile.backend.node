FROM node:25-slim

ENV SALES_DASHBOARD_NODE_DB_PATH=/data/sales_dashboard_node.db
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY server ./server
RUN mkdir -p /data

EXPOSE 3002
CMD ["npm", "run", "api:node"]
